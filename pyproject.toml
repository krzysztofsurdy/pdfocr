[project]
name = "pdfocr"
version = "0.1.0"
description = "PDF to Markdown converter using Docling"
requires-python = ">=3.10"
dependencies = [
    "docling",
    "rich",
]

[project.scripts]
pdfocr = "convert:main"
